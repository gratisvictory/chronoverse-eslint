/**
 * Glob patterns for JavaScript files (includes .js, .mjs, .cjs, .jsx).
 * @type {string[]}
 */
export const JAVASCRIPT_FILES = ['**/*.?([cm])js?(x)'];

/**
 * Glob patterns for TypeScript files (includes .ts, .mts, .tsx).
 * @type {string[]}
 */
export const TYPESCRIPT_FILES = ['**/*.?([m])ts?(x)'];

/**
 * Glob patterns for CSS files.
 * @type {string[]}
 */
export const CSS_FILES = ['**/*.css'];
